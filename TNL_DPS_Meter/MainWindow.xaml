<Window x:Class="TNL_DPS_Meter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TNL_DPS_Meter"
        mc:Ignorable="d"
        Title="TNL DPS Meter"
        Height="130"
        Width="266"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#4D000000"/>
        <SolidColorBrush x:Key="TabBackgroundBrush" Color="#AA333333"/>
        <SolidColorBrush x:Key="SelectedTabBrush" Color="#FF555555"/>
        <SolidColorBrush x:Key="TextBrush" Color="White"/>
        <SolidColorBrush x:Key="FlashBrush" Color="#FFAA00FF"/>

        <Style x:Key="CustomComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource TabBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Grid.Column="0"
                                                    Margin="5,0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>

                                    <ToggleButton Grid.Column="1"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Foreground="{StaticResource TextBrush}"
                                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <TextBlock Text="▼" FontSize="8" VerticalAlignment="Center"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <Popup x:Name="Popup"
                                   Placement="Top"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide"
                                   MinWidth="{TemplateBinding ActualWidth}"
                                   Width="{TemplateBinding ActualWidth}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource TabBackgroundBrush}"
                                        BorderBrush="#FF666666"
                                        BorderThickness="1"
                                        CornerRadius="3">
                                    <ScrollViewer MaxHeight="120"
                                                  VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ScrollViewer.Resources>
                                            <Style TargetType="ScrollBar">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Width" Value="12"/>
                                            </Style>
                                        </ScrollViewer.Resources>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedTabBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="5,3">
                            <ContentPresenter/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedTabBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedTabBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder"
            Background="{StaticResource WindowBackgroundBrush}"
            BorderBrush="#FF666666"
            BorderThickness="1"
            CornerRadius="5"
            MouseLeftButtonDown="Window_MouseLeftButtonDown"
            MouseEnter="Window_MouseEnter"
            MouseLeave="Window_MouseLeave">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tab Headers (hidden when mouse leaves) -->
            <Border x:Name="TabHeaders" Grid.Row="0"
                    Background="{StaticResource TabBackgroundBrush}"
                    CornerRadius="5,5,0,0"
                    Height="25">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="ViewSelector"
                             Grid.Column="0"
                             Margin="10,0,0,0"
                             Width="140"
                             Height="20"
                             FontSize="9"
                             Style="{StaticResource CustomComboBoxStyle}"
                             ItemContainerStyle="{StaticResource CustomComboBoxItemStyle}"
                             SelectionChanged="ViewSelector_SelectionChanged">
                        <ComboBoxItem Content="Last Combat" IsSelected="True"/>
                        <ComboBoxItem Content="Overall Damage"/>
                    </ComboBox>

                    <Button Grid.Column="1"
                            Content="✕"
                            Background="Transparent"
                            Foreground="{StaticResource TextBrush}"
                            BorderThickness="0"
                            Width="25"
                            Height="25"
                            Click="CloseButton_Click"
                            FontSize="12"
                            Margin="0"/>
                </Grid>
            </Border>

            <!-- DPS Content (always visible) -->
            <Grid Grid.Row="1" Background="{StaticResource TabBackgroundBrush}">
                <!-- Last Combat View -->
                <StackPanel x:Name="LastCombatView"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Visibility="Visible">
                    <TextBlock x:Name="CurrentCombatText"
                             Text="0 | 0.0"
                             FontSize="24"
                             FontWeight="Bold"
                             Foreground="{StaticResource TextBrush}"
                             HorizontalAlignment="Center"/>
                    <TextBlock x:Name="LastCombatTimeText"
                             Text="00:00:00:000"
                             FontSize="12"
                             Foreground="#FFAAAAAA"
                             HorizontalAlignment="Center"
                             Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Overall Damage View -->
                <StackPanel x:Name="OverallDamageView"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Visibility="Collapsed">
                    <TextBlock x:Name="OverallDamageText"
                             Text="0 | 0.0"
                             FontSize="24"
                             FontWeight="Bold"
                             Foreground="{StaticResource TextBrush}"
                             HorizontalAlignment="Center"/>
                    <TextBlock x:Name="OverallTimeText"
                             Text="00:00:00:000"
                             FontSize="12"
                             Foreground="#FFAAAAAA"
                             HorizontalAlignment="Center"
                             Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <!-- File Info -->
            <Border x:Name="FileInfoContainer" Grid.Row="2"
                    Background="{StaticResource TabBackgroundBrush}"
                    Height="25"
                    CornerRadius="0,0,5,5">
                <TextBlock x:Name="FileInfoText"
                          Text="No log file"
                          FontSize="9"
                          Foreground="#FFAAAAAA"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="5"/>
            </Border>
        </Grid>
    </Border>
</Window>
